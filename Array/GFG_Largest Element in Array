
class Solution:
    def largest(self, arr : List[int]) -> int:
        # code here
        max1 = arr[0]
        
        for i in arr[1:]:
            max1 = max(max1,i)
        return max1
        